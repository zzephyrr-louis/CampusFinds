# Use local browser-based responses while the Express/database backend is paused.
# Change this to "server" when the MySQL backend is ready.
VITE_API_MODE=server
VITE_API_URL=http://localhost:8080/api
