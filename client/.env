# Use local browser-based responses while the Spring/MySQL backend is unavailable.
# Change this to "server" after the backend and database are running.
VITE_API_MODE=mock
VITE_API_URL=http://localhost:8080/api
